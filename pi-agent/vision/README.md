# Vision & Object Detection Module (Placeholder)

This module will run real-time AI object detection (YOLOv8-Nano, OpenCV, TFLite) on the Pi video stream.

Future integration path:
- Bounding box inference
- Metadata broadcast over WebSocket (`detection:bbox`)
- Canvas overlay on Next.js video component
