# Rover Motor Control Module (Placeholder)

This module will interface with GPIO motor drivers (L298N, PCA9685 PWM, or CAN bus) for rover movement.

Future integration path:
- Direction commands (`FORWARD`, `BACKWARD`, `LEFT`, `RIGHT`, `STOP`)
- Speed regulation (PWM control)
- Low latency WebSocket control protocol
