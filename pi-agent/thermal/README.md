# Thermal Module (Placeholder)

This module will handle MLX90640 / Lepton thermal camera frame capture and heatmap telemetry over WebSocket.

Future integration path:
- Thermal matrix read (e.g. 24x32 or 80x60 array)
- Color map generation & WebSocket JSON stream
- Next.js heatmap visualizer rendering
